document.addEventListener("DOMContentLoaded", () => {
    // Aquí se pueden agregar funciones para cargar datos dinámicamente,
    // enviar formularios mediante fetch/ajax, validar inputs, etc.
    console.log("JS cargado correctamente.");
});
